# Flutter Firestore Feed

Simple feed app with real-time posts and comments.

Steps:
1. Add your google-services.json (Android) / GoogleService-Info.plist (iOS).
2. Run `flutter pub get`.
3. Run the app with `flutter run`.
4. Add posts using the + FAB and tap on a post to see/add comments.
